---
description: Run the project's test suite and summarize results
---

Run the tests for this project and report results:

1. Detect the test framework (Jest, pytest, go test, cargo test, etc.) by inspecting `package.json`, `pyproject.toml`, or other config files
2. Run the appropriate test command
3. Summarize: total tests, passed, failed, skipped
4. For any failures, show the test name, error message, and file location
5. Suggest fixes for any failing tests if the cause is clear
